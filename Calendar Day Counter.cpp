#include <iostream>
#include <iomanip>

using namespace std;

int main()
{
	//Varibles
	int month, year;
	char again;

	//Do-while loop to repeat the program while again is 'y' or 'Y'
	do
	{

	} while (again == 'y' || again == 'Y');

    return 0;
}
